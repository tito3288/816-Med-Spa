---
const navLinks = [
  { href: "/", label: "Home" },
  { href: "#about", label: "About" },
  { href: "#conditions", label: "Services" },
  { href: "#conditions", label: "Concerns" },
  { href: "#treatments", label: "Treatments" },
  { href: "#gallery", label: "Gallery" },
  { href: "#contact", label: "Contact" },
];
---

<header
  class="fixed inset-x-0 top-0 z-50 border-b border-transparent bg-transparent text-cream transition-colors duration-300"
  data-site-header
>
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Mobile / tablet: single row -->
    <div class="flex items-center justify-between py-4 md:py-5 lg:hidden">
      <a href="/" class="shrink-0" aria-label="816 Med Spa home">
        <img src="/logo.png" alt="816 Med Spa" class="h-16 w-auto sm:h-20" />
      </a>
      <div class="flex items-center gap-3">
        <a
          href="#contact"
          class="hidden border border-cream/70 bg-transparent px-4 py-2.5 text-sm font-medium text-cream transition hover:bg-cream hover:text-espresso sm:inline-block"
          data-header-cta
        >
          Request Appointment
        </a>
        <button
          type="button"
          class="p-2 text-current transition hover:text-sage"
          aria-label="Open menu"
          aria-expanded="false"
          aria-controls="mobile-menu"
          data-mobile-menu-button
        >
          <svg
            class="h-6 w-6"
            data-icon="open"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
            aria-hidden="true"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
          <svg
            class="hidden h-6 w-6"
            data-icon="close"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
            aria-hidden="true"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M6 6l12 12M6 18L18 6"></path>
          </svg>
        </button>
      </div>
    </div>

    <!-- Desktop: centered logo + nav row -->
    <div class="hidden lg:block py-6 transition-[padding] duration-300" data-desktop-header-inner>
      <div class="grid grid-cols-[1fr_auto_1fr] items-center">
        <div aria-hidden="true"></div>
        <a href="/" class="justify-self-center" aria-label="816 Med Spa home">
          <img
            src="/logo.png"
            alt="816 Med Spa"
            class="h-20 w-auto transition-all duration-300 xl:h-24"
            data-header-logo
          />
        </a>
        <a
          href="#contact"
          class="justify-self-end border border-cream/70 bg-transparent px-6 py-2.5 text-xs font-medium tracking-[0.12em] text-cream uppercase transition hover:bg-cream hover:text-espresso"
          data-header-cta
        >
          Request Appointment
        </a>
      </div>
      <nav
        class="mt-4 flex flex-wrap items-center justify-center gap-x-6 gap-y-2 transition-[margin] duration-300 xl:gap-x-9"
        aria-label="Primary"
        data-header-nav
      >
        {navLinks.map((link) => (
          <a
            href={link.href}
            class="font-body text-sm font-medium tracking-[0.16em] text-current uppercase transition hover:text-sage focus-visible:text-sage"
            data-header-nav-link
          >
            {link.label}
          </a>
        ))}
      </nav>
    </div>

    <div id="mobile-menu" class="hidden pb-4 lg:hidden" data-mobile-menu>
      <nav class="flex flex-col gap-1 border-t border-current/20 pt-4" aria-label="Mobile">
        {navLinks.map((link) => (
          <a
            href={link.href}
            class="px-2 py-3 text-base font-medium text-current transition hover:text-sage focus-visible:text-sage"
          >
            {link.label}
          </a>
        ))}
        <a
          href="#contact"
          class="mt-3 border border-cream/70 bg-transparent px-6 py-3 text-center font-medium text-cream transition hover:bg-cream hover:text-espresso"
          data-header-cta
        >
          Request Appointment
        </a>
      </nav>
    </div>
  </div>
</header>

<script>
  const header = document.querySelector<HTMLElement>("[data-site-header]");
  const headerCtas = document.querySelectorAll<HTMLElement>("[data-header-cta]");
  const headerNavLinks = document.querySelectorAll<HTMLElement>("[data-header-nav-link]");
  const headerLogo = document.querySelector<HTMLElement>("[data-header-logo]");
  const desktopHeaderInner = document.querySelector<HTMLElement>("[data-desktop-header-inner]");
  const headerNav = document.querySelector<HTMLElement>("[data-header-nav]");
  const button = document.querySelector<HTMLButtonElement>("[data-mobile-menu-button]");
  const menu = document.querySelector<HTMLElement>("[data-mobile-menu]");

  const setHeaderAtTop = () => {
    if (!header) return;

    const menuOpen = menu ? !menu.classList.contains("hidden") : false;
    const atTop = window.scrollY < 12 && !menuOpen;

    header.classList.toggle("border-transparent", atTop);
    header.classList.toggle("bg-transparent", atTop);
    header.classList.toggle("text-cream", atTop);
    header.classList.toggle("border-sage/20", !atTop);
    header.classList.toggle("bg-white", !atTop);
    header.classList.toggle("text-gray-900", !atTop);
    header.classList.toggle("shadow-sm", !atTop);

    desktopHeaderInner?.classList.toggle("py-6", atTop);
    desktopHeaderInner?.classList.toggle("py-4", !atTop);
    headerLogo?.classList.toggle("h-20", atTop);
    headerLogo?.classList.toggle("xl:h-24", atTop);
    headerLogo?.classList.toggle("h-14", !atTop);
    headerLogo?.classList.toggle("xl:h-16", !atTop);
    headerNav?.classList.toggle("mt-4", atTop);
    headerNav?.classList.toggle("mt-2", !atTop);

    headerNavLinks.forEach((link) => {
      link.classList.toggle("text-sm", atTop);
      link.classList.toggle("tracking-[0.16em]", atTop);
      link.classList.toggle("text-xs", !atTop);
      link.classList.toggle("tracking-[0.14em]", !atTop);
    });

    headerCtas.forEach((cta) => {
      cta.classList.toggle("border-cream/70", atTop);
      cta.classList.toggle("bg-transparent", atTop);
      cta.classList.toggle("text-cream", atTop);
      cta.classList.toggle("hover:bg-cream", atTop);
      cta.classList.toggle("hover:text-espresso", atTop);
      cta.classList.toggle("border-sage", !atTop);
      cta.classList.toggle("bg-sage", !atTop);
      cta.classList.toggle("text-white", !atTop);
      cta.classList.toggle("hover:bg-sage/90", !atTop);
    });
  };

  setHeaderAtTop();
  window.addEventListener("scroll", setHeaderAtTop, { passive: true });

  if (button && menu) {
    const openIcon = button.querySelector<SVGElement>('[data-icon="open"]');
    const closeIcon = button.querySelector<SVGElement>('[data-icon="close"]');

    const setOpen = (open: boolean) => {
      menu.classList.toggle("hidden", !open);
      button.setAttribute("aria-expanded", String(open));
      button.setAttribute("aria-label", open ? "Close menu" : "Open menu");
      openIcon?.classList.toggle("hidden", open);
      closeIcon?.classList.toggle("hidden", !open);
      setHeaderAtTop();
    };

    button.addEventListener("click", () => {
      setOpen(menu.classList.contains("hidden"));
    });

    document.addEventListener("keydown", (event) => {
      if (event.key === "Escape" && !menu.classList.contains("hidden")) {
        setOpen(false);
        button.focus();
      }
    });

    menu.querySelectorAll("a").forEach((link) => {
      link.addEventListener("click", () => setOpen(false));
    });
  }
</script>
